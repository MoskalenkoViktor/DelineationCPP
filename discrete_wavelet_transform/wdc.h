#pragma once
#include <vector>


std::vector<std::vector<double>> get_wdc(const std::vector<double>& signal_)
{
    /*std::vector<double> signal(signal_);
    const size_t NUM_WDC_SCALES = 5;
    const size_t dwt_segment = static_cast<size_t>(std::pow(2, NUM_WDC_SCALES));
    const size_t dwt_shift = signal.size() % dwt_segment;
    if (dwt_segment != 0)
    {
        signal = std::vector<double>(signal.begin(), signal.end() - dwt_shift);
    }*/
    return {};
}
